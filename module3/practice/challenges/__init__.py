"""ЧЕЛЕНДЖІ · інфраструктура пошуку, перенесена з практики модуля 4 без змін.

    qdrant_store.py    сховище фрагментів у Qdrant (Docker), $0; --up, --info,
                       --migrate, --check, --down
    spec_download.py   уся специфікація ECMA-262 у docs-full/, $0
    suite_download.py  ECMA-402/404/414 і документи довкола 402 у docs-suite/, $0
    live_fetch.py      живий сайт tc39.es для спеціаліста EXOTIC, лише з --live

Власних челенджів у модулі 3 тут немає: необов'язкові пункти його картки —
межі кодом, пауза перед незворотною дією, одне питання через два стеки —
живуть у base/graph.py і base/compare.py, бо це властивості самого переносу,
а не окремі вправи.
"""
