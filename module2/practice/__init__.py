r"""
Практика модуля 2 — домашнє завдання курсу.

Живе всередині module2/, але НЕ є частиною курсового матеріалу: жоден курсовий
файл тут не змінюється. Практика перевикористовує ядро (`core/agent.py`,
`core/cost.py`) і додає власний корпус документів, два пошуки по ньому і один
інструмент, яким агент цим пошуком користується.

ЩО ЗА ДОКУМЕНТИ

Вісімнадцять розділів специфікації ECMAScript навколо `sec-object-type`, узятих
з https://tc39.es/ecma262/: сам тип Object, його внутрішні методи й інваріанти,
екзотичні об'єкти (Array, String, Arguments, TypedArray, Proxy, зв'язані функції,
простори імен модулів, незмінні прототипи) і об'єктні обгортки String, Symbol,
Number, Boolean. Лежать у practice/docs/ як .txt із трирядковою шапкою; поруч
index.json з переліком і адресами джерел. Завантажені один раз вручну — код до
мережі по них не ходить.

Запити до агента англійські, бо англійський корпус: питати про специфікацію
українською можна (модель ембедингів багатомовна), але тоді до різниці «слова
проти змісту» додається ще й різниця мов, і незрозуміло, що саме показує прогін.

ЧИМ ЦЕ ВІДРІЗНЯЄТЬСЯ ВІД КУРСОВОГО МОДУЛЯ 2

Курсовий `modules/m02_rag.py` кладе знайдене в системний промпт до початку роботи
агента — правила приїжджають завжди, потрібні вони чи ні. Тут пошук відданий
агентові інструментом `search_docs`, і він сам вирішує, що і скільки разів
шукати. Курсова база знань — чотирнадцять коротких правил із рукописними
ключовими словами; тут — вісімнадцять справжніх документів, які руками не
розмічені й розмічені бути не можуть.

ЯК ЗАПУСКАТИ

Усе — з теки module2/ і інтерпретатором з .venv; нижче для стислості написано
просто `python`, читайте як `.venv/bin/python`. Кожна точка входу є модулем ЦЬОГО
пакета, тому й викликається через `python -m practice.<модуль>`, а не як окремий
файл. З кореня репозиторію не запуститься нічого: `config`, `core`, `domain`
шукаються від module2/. Позначка $0 означає, що прогін до моделі Anthropic не
звертається і грошей не коштує.

БЕЗ ГРОШЕЙ — усе, що стосується самого пошуку:

    python -m practice.base.compare         # BM25 проти ембедингів, $0
    python -m practice.base.compare --full  # те саме з текстом фрагментів, $0
    python -m practice.base.threshold       # звідки взявся поріг відмови, $0
    python -m practice.challenges.a_chunking       # розмір шматка, $0

Перший із цих прогонів завантажує ваги моделі e5-small (близько 470 МБ) і рахує
індекс корпусу. Це єдиний раз, коли потрібна мережа; далі вектори беруться з
кеша в practice/index/.

З ГРОШИМА — агент:

    python -m practice.base.ask             # сценарій replace, ~$0.02
    python -m practice.base.ask proxy       # складене питання, два пошуки
    python -m practice.base.ask absent      # питання не з корпусу — має відмовити
    python -m practice.base.ask known       # питання, яке модель знає з голови
    python -m practice.base.ask thin        # слабке зачеплення пошуку
    python -m practice.base.ask known --lexical   # той самий сценарій на BM25
    python -m practice.base.ask known --rewrite   # з переписуванням запиту
    python -m practice.base.ask --list      # перелік сценаріїв, $0

Результати прогонів лягають у practice/out/practice_results.json під ключем
«сценарій:пошук». Повторний прогін тієї самої пари замінює свій попередній
запис, чужих не чіпає.

СТРУКТУРА

    docs/       вісімнадцять документів і index.json — вхідні дані, не код
    index/      кеш векторів; створюється сам, у git не потрапляє
    out/        результати прогонів агента
    common/     корпус, лексичний пошук, векторний пошук, інструмент, промпт,
                переписування запиту
    base/       точки входу
    challenges/ необов'язкові пункти картки

ЩО ЗАЛИШИЛОСЯ ПОЗА МЕЖАМИ

Усі три необов'язкові пункти картки зроблені, і кожен має своє обмеження,
назване в CHECKLIST.md і README.md. Коротко: дослід з межею розміру шматка
показав, що дрібніші шматки тут шкодять; переписування запиту працює в пошуку,
але в агентському контурі здебільшого дублює те, що агент робить сам; посилання
на джерело агент дає, хоч в одному прогоні з двох скоротив його до номера
розділу.
"""
