"""Спільна підготовка тестів.

Потрібна рівно для одного: додати корінь module7/ у sys.path, щоб `practice`,
`core` і `config` імпортувалися як пакети верхнього рівня незалежно від того, з
якої теки запущено pytest.

Нічого іншого тут не робиться. Перевірки набору і правил читають набір кейсів та
паспорт корпусу з `data/`, обидва під git, тому вони проходять і на голому клоні.
Перевірки над прогоном шукають файл прогону в `out/` і пропускаються, коли його
там немає.
"""

import pathlib
import sys

sys.path.insert(0, str(pathlib.Path(__file__).resolve().parents[1]))   # module7/
