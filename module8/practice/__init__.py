"""
Практика модуля 8: агент специфікації як сервіс — та сама логіка, інший спосіб запуску.

Агент живе тут же, поруч: сервер знань (base/spec_server.py), чотири шари
оборони (base/layers.py) і сам агент (base/agent.py) — усе в цій теці, як у
практиках модулів 6 і 7. Нічого поза модулем практика не потребує: корпус лежить
у docs-full/ і docs-suite/, ключ береться з module8/.env.

Модуль не змінює самого агента — він міняє те, де агент працює. Досі агент жив
у терміналі й помирав разом із ним; тут він стає HTTP-сервісом (api.py), їде в
контейнер і в Kubernetes разом зі своєю векторною базою (api.py, deploy/), і його
можна викликати з чужого комп'ютера, не маючи ні цієї теки, ні цього ноутбука.
"""
