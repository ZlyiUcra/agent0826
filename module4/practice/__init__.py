r"""
Практика модуля 4 — домашнє завдання курсу.

Живе всередині module4/, але НЕ є частиною курсового матеріалу: жоден курсовий
файл тут не змінюється. Практика перевикористовує ядро (`core/agent.py`,
`core/cost.py`) і будує поверх нього мультиагентну систему: маршрутизатор на
дешевій моделі, кілька профільних агентів під ним і другий патерн усередині
одного з них.

СТАН НА 2026-08-25

Зроблено тільки підготовку. Перенесено документи й шар пошуку з практики модуля
2; сам маршрутизатор, профільні агенти й вимірювання ще не написані. Що саме
вимагає картка і чого ще немає — у CHECKLIST.md, там усі дванадцять чекбоксів
поки порожні.

ЩО ЗА ДОКУМЕНТИ

Ті самі вісімнадцять розділів специфікації ECMAScript навколо `sec-object-type`,
що й у практиці модуля 2, узяті з https://tc39.es/ecma262/. Лежать у
practice/docs/ як .txt із трирядковою шапкою; поруч index.json з переліком і
адресами джерел. Завантажені один раз вручну — код до мережі по них не ходить.

Документи скопійовані, а не спільні з модулем 2. Так у цьому репозиторії
влаштовано все: `core/`, `domain/` і `config.py` теж лежать у кожному модулі
своєю копією, побайтово однаковою. Модуль має запускатися сам по собі, не
знаючи про сусідів.

ЧИМ ЦЕ ВІДРІЗНЯТИМЕТЬСЯ ВІД КУРСОВОГО МОДУЛЯ 4

Курсовий `modules/m04_orchestration.py` маршрутизує запит на чотири профільні
промпти (STATUS, FINANCE, CLAIM, HUMAN), але всі чотири агенти отримують
ОДНАКОВИЙ набір інструментів: кожен викликає `tools_for(4)`, а це повний список.
Картка вимагає протилежного — різних інструментів і різних прав у різних
спеціалістів, з відповіддю на питання «чому саме такі». Це і є місце для власної
роботи.

ЩО ВЖЕ ПЕРЕНЕСЕНО

    docs/       вісімнадцять документів і index.json — вхідні дані, не код
    common/     corpus.py, lexical.py, vectors.py, rewrite.py — шар пошуку,
                скопійований з практики модуля 2 без змін

Шар пошуку перенесено цілком, бо він нічого не знає про оркестрацію: читає
документи, ділить на фрагменти, шукає по словах (BM25) і по змісту (ембединги).
Файл `tools.py` з практики модуля 2 НЕ перенесено навмисно — там живе рішення
про те, який інструмент дістає агент, а саме це в модулі 4 має бути іншим у
кожного спеціаліста.

ЩО ЩЕ НЕ НАПИСАНО

    base/       порожньо — точок входу немає
    challenges/ порожньо — жоден необов'язковий пункт не починався

ЯК ЗАПУСКАТИ

Коли з'являться точки входу — з теки module4/ і інтерпретатором з віртуального
середовища; кожна точка входу буде модулем ЦЬОГО пакета, тобто викликатиметься
через `python -m practice.<модуль>`, а не як окремий файл. З кореня репозиторію
не запуститься нічого: `config`, `core`, `domain` шукаються від module4/.
"""
