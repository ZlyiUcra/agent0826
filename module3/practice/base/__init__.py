"""ОСНОВА · точки входу практики. Див. довідник у practice/__init__.py.

    graph.py     НОВЕ: та сама система на LangGraph — маршрутизатор → спеціаліст
                 → запасний маршрут → критик → три паралельні гілки замість
                 передачі людині → передача лише після провалу всіх; --show
                 малює вузли й ребра, $0; --pause зупиняє граф перед передачею;
                 --alt додає гілки і до вдалої відповіді
    chat.py      НОВЕ: бесіда з графом на багато реплік — історія в
                 чекпоінтері, пам'ять з context/memory.py, /alt
    compare.py   НОВЕ: ті самі запити через ручний стек і через граф — час,
                 вартість, рішення; --loc рахує рядки коду, $0
    smoke.py     НОВЕ: безкоштовні перевірки переносу, $0
    system.py    ручний стек з модуля 4 без змін — друга сторона порівняння
    team.py      спеціалісти: родини документів, інструменти, права — з модуля 4
    critic.py    звірка посилань ($0) і критик чернетки — з модуля 4
    queries.py   п'ять запитів виміру — з модуля 4
    single.py    один агент без оркестрації — з модуля 4; потрібен чатові

Платять гроші лише graph, chat, system, single і compare — і лише коли їх
запустили руками.
"""
