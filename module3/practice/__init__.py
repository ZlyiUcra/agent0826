r"""
Практика модуля 3 — домашнє завдання курсу: той самий агент на іншому стеку.

Живе всередині module3/, але НЕ є частиною курсового матеріалу: жоден курсовий
файл тут не змінюється. Картка модуля просить узяти свого агента з модуля 1
або 2 і перенести його на стек, якого ще не пробували, — не переписати, а
перенести: та сама задача, ті самі інструменти, та сама поведінка.

СТАН НА 2026-08-26

Агент — довідник зі специфікації ECMAScript з практики модуля 2, у тому
вигляді, якого він набув у модулі 4: маршрутизатор на дешевій моделі, три
тематичні спеціалісти, загальний маршрут із субагентом, критик, детермінована
передача людині. Перенесено на LangGraph: маршрут став графом із п'ятьма
вузлами, цикл «модель ↔ інструменти» — підграфом, пауза перед передачею
людині — зупинкою графа з продовженням у тому самому процесі. Ручний стек
лежить поруч без змін — як друга сторона порівняння. Безкоштовні перевірки
зелені; платні прогони — у README, розділ «Стан на сьогодні».

ЩО В ТЕЦІ ЗВІДКИ

    docs/, docs-full/, docs-suite/   три набори документів специфікації — з
                                     практики модуля 4, побайтово
    common/      шар пошуку (корпус, вектори, лексика, пульс) — з модуля 4
    challenges/  сховище Qdrant, завантажувачі документів, живий сайт — з модуля 4
    base/team.py, critic.py, queries.py, system.py, single.py
                 спеціалісти, критик, запити виміру, РУЧНИЙ стек — з модуля 4
    base/graph.py     НОВЕ: та сама система на LangGraph, з паралельними
                      гілками замість передачі людині
    base/chat.py      НОВЕ: бесіда з графом на багато реплік, пам'ять з context/, /alt
    base/compare.py   НОВЕ: обидва стеки на тих самих запитах, час і вартість
    base/smoke.py     НОВЕ: безкоштовні перевірки переносу
    context/     чат і пам'ять — друга картка модуля 4, перенесено без змін

Документи скопійовані, а не спільні з модулем 4: модуль має запускатися сам по
собі, не знаючи про сусідів. Оскільки документи ті самі, колекції в Qdrant
теж ті самі (spec-core-e5, spec-full-e5, spec-suite-e5) — заливати їх удруге
не треба.

ЯК ЗАПУСКАТИ

З теки module3/ і інтерпретатором з віртуального середовища; кожна точка входу
— модуль ЦЬОГО пакета, тобто викликається через `python -m practice.base.<модуль>`,
а не як окремий файл. З кореня репозиторію не запуститься нічого: `config`,
`core`, `domain` шукаються від module3/. Установка середовища і повний перелік
команд — у SETUP.md.
"""


def _load_env() -> None:
    """Підтягує module3/.env, щоб прогони практики бачили свої налаштування —
    QDRANT_URL, PRACTICE_DOCS, PRACTICE_RETRIEVER.

    Те саме робить config.py, але він вимагає ключа Anthropic і завершує процес,
    коли ключа немає. Безкоштовним прогонам ключ не потрібен, а налаштування
    потрібні, тож читання .env живе тут окремо. Значення, задані в оточенні,
    сильніші за файл: load_dotenv наявних змінних не перезаписує.
    """
    try:
        from dotenv import load_dotenv
    except ImportError:
        return
    import pathlib as _pathlib
    load_dotenv(_pathlib.Path(__file__).resolve().parent.parent / ".env")


_load_env()
