"""ОСНОВА · точки входу практики. Див. довідник у practice/__init__.py.

    queries.py   п'ять запитів виміру, зафіксовані до прогонів, $0
    team.py      спеціалісти: родини документів, інструменти, права і чому такі
    critic.py    звірка посилань ($0, всі маршрути) і петля з критиком (WRAPPERS)
    system.py    маршрутизатор → спеціаліст → перевірки → передача людині
    single.py    один агент без оркестрації — точка відліку порівняння
    compare.py   п'ять запитів через обох, час і вартість по кожному
    smoke.py     безкоштовні перевірки дизайну; --warm збирає індекси
    probe.py     що піднімає пошук на запит, тим самим індексом, що й спеціалісти, $0

Платять гроші лише system, single і compare — і лише коли їх запустили руками.
"""
